<?xml version="1.0" encoding="UTF-8"?>
<!-- this file only for development trouble shooting, the production logback configuration should be defined in helm-chart.-->
<configuration>

    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } [%15.15t]: %-40.40logger{100} :  %m%n"/>

    <!-- console output format -->
    <appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="stdout"></appender-ref>
    </root>

</configuration>
